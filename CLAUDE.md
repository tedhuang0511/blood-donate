## Language

一律使用繁體中文與使用者對話,除非使用者主動改用其他語言。

## Agent skills

### Issue tracker

Issues live in GitHub Issues (repo: `tedhuang0511/blood-donate`), managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
